name: Secrets As Env Vars
on:
  workflow_dispatch
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Use Secret As Env
        env: 
           MESSAGE: ${{ secrets.MY_SECRET_MESSAGE }}
        run: |
          echo "Username exists: $MESSAGE"
