name: Run Test
on: 
  push:
jobs: 
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v4
      - name: Run Tests
        run: |
          echo "Running Tests...."
          echo "All Test Passed"
