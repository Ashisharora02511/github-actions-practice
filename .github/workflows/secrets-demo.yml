
name: Secrets Demo
on: 
  workflow_dispatch:
jobs:
  test-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Check Secret
        run: |
          echo "The secret is set: ${{ secrets.MY_SECRET_MESSAGE }}"
